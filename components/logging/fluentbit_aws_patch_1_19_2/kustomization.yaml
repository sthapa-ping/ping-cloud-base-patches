apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

generatorOptions:
  labels:
    k8s-app: fluent-bit

configMapGenerator:
  - name: fluent-bit-pipeline-elk
    behavior: replace
    files:
      - elk.conf          
patchesJson6902:
- target:
    group: apps
    version: v1
    kind: DaemonSet
    name: fluent-bit
  path: patch-remove-liveness-httpget.yaml

patchesStrategicMerge:
  - fluentbit-config-patch.yaml
  - fluentbit-patch.yaml
